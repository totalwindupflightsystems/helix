# GAP-009 — diagnostics.md board-format stale reference

`docs/dogfood/diagnostics.md:23` still described the task board as
`.coding-hermes/board/board.db (DuckDB)` — stale after the JSONL-NORM-001
migration to JSONL canonical storage.

## Change
Rewrite the Board section to reference `.coding-hermes/board/tasks.jsonl`
(JSONL canonical) with the supporting JSONL files; remove all `board.db`
mentions.

## Acceptance
`grep 'board\.db' docs/dogfood/diagnostics.md` returns empty.
