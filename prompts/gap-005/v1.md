# GAP-005 — Unified CLI unreachable outside repo root

## Task (verbatim from board)

GAP-005 — Unified CLI unreachable outside repo root: helix estimate/identity/sandbox shell out to sibling binaries; from any other dir: subcommand "helix-estimate" not found.

**PASS criteria:** `cd /tmp && /home/kara/helix/helix estimate estimate "hello" --model deepseek-v4-pro --provider deepseek` returns a cost estimate (not "subcommand not found").

## Design

Extend `lookPath` (cmd/helix/main.go:690) to resolve delegated subcommand binaries next to the running helix executable (`os.Executable()` dir) before falling back to cwd-relative and PATH lookups, so the unified CLI works from any directory. Add tests; keep existing lookPath behavior tests passing.

## Status

- 2026-08-07: dispatched (tick #101, worker deepseek-v4-flash @ deepseek-foreman).
