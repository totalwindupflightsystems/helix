# Prompt: SRC-004 — Per-Source Rate Limiting (v1)

**Task:** SRC-004 — Per-source rate limiting (`pkg/source/ratelimit.go`) — rate-limit wrapper per source.

**Spec:** SPEC-025 §3 (rate_limit: 10/s in source YAML) + §6 (Rate Limiter: extend pkg/forgejo/rate.go token bucket for sources) + §8 (Files).

**Date:** 2026-08-04 (foreman tick #79)

**Worker model:** deepseek-v4-flash @ deepseek-foreman

## Prompt highlights

- Implement `pkg/source/ratelimit.go` + `pkg/source/ratelimit_test.go` per SPEC-025.
- Parse `Source.RateLimit` (config.go already has the `rate_limit` YAML field, e.g. `10/s`) into per-source token-bucket limiters.
- Extend the TokenBucket pattern from `pkg/forgejo/rate.go` (golang.org/x/time/rate, already in go.mod).
- Concurrent-safe per-source limiter manager; empty/invalid rate_limit string = no limit.
- Full prompt: `.coding-hermes/board/` events + DuckBrain `/project/helix/` (tick #79 record).
