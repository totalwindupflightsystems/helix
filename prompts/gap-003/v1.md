# GAP-003 — No onboarding docs

Task: docs/GETTING-STARTED.md does not exist; README references the `--spec`
flag (actual `--spec-file`) and Docker port 3000 (actual host port 3030). A new
user must reverse-engineer the platform from AGENTS.md + `--help`.

PASS criteria:
- docs/GETTING-STARTED.md exists, covering: build, Forgejo URL (:3030), helix
  doctor, estimate, prompt register.
- README `--spec` / port-3000 drift gone (README, scripts/up.sh,
  docker-compose.yml all document host port 3030; container-internal 3000 and
  the compose-network FORGEJO_URL stay untouched).

Resolution (tick #99, foreman-direct docs task, spec/doc judge exception):
- Wrote docs/GETTING-STARTED.md grounded in the live CLI surface
  (`./helix --help`, `helix estimate --help`, `helix prompt --help`,
  `helix doctor` run against live Forgejo :3030).
- `--spec` README drift was already fixed by DF-003 (README examples use
  `estimate estimate|check ... --model --provider`; grep for `--spec` in
  README.md = 0 matches) — verified, no change needed.
- Port drift fixed at 6 sites: README.md ×2 (provision example, service
  table), scripts/up.sh ×2 (readiness probe, UI banner), docker-compose.yml
  ×2 (header comment, `ports: "3030:3000"`). Container-internal healthcheck
  (localhost:3000) and compose-network FORGEJO_URL (forgejo:3000) left as-is —
  they are correct inside the docker network.
