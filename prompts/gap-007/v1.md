# GAP-007 — Dispatcher cannot decompose Helix's own spec files

## Task (verbatim from board)

P1 Dispatcher cannot decompose Helix's own spec files — pkg/dispatcher/decomposer.go:54 requires H2 headings containing 'Phase'/'Feature', but specs/ (e.g. specs/agent-identity.md) use `# Helix Feature N` H1 titles with H2 sections like '## Security Model' — `./helix dispatcher list-tasks --spec specs/agent-identity.md` errors 'no Phase or Feature sections found'. Hunter-verified 2026-08-07 (build/tests/lint all green). Fix: accept `^# Helix Feature` H1 patterns as section markers (or add Phase H2 sections to specs). PASS: `./helix dispatcher list-tasks --spec specs/agent-identity.md` returns a decomposed task list instead of erroring.

## Design

Extend `DecomposeSpec` (pkg/dispatcher/decomposer.go) to treat an H1 heading matching `^# Helix Feature` (case-insensitive) as a section marker — each feature H1 starts one Task, priority assigned by section ordering. Existing `## PHASE`/`## Feature` H2 detection is unchanged. No spec-file edits (6 specs use the H1 layout; the decomposer should handle the repo's own spec format). Add a unit test locking the H1 behavior; keep existing tests passing.

## Status

- 2026-08-08: implemented foreman-direct (tick #107, Exception 7 — single package, clear AC, <30 lines, no new deps). Live-verified: all 6 `# Helix Feature` specs decompose to 1 task each; no-section error path preserved (exit 2).
